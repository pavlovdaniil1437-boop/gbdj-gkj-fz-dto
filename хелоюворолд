using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class xelouvorold : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("xelouvorold");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
